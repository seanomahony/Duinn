<?xml version="1.0" encoding="UTF-8"?>
<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:Duinn.Views"
       x:Class="Duinn.AppShell">

    <TabBar>
        <ShellContent Title="Calendar"
                      Icon="bear.png"
                      ContentTemplate="{DataTemplate views:HomePage}" 
                      Style="{StaticResource Key=ShellStyle}"/>
        <ShellContent Title="Events List"
                      Icon="elephant.png"
                      ContentTemplate="{DataTemplate views:EventsList}" 
                      Style="{StaticResource Key=ShellStyle}"/>
        <ShellContent Title="Add Event"
                      Icon="monkey.png"
                      ContentTemplate="{DataTemplate views:AddEvent}"
                      Style="{StaticResource Key=ShellStyle}"/>
    </TabBar>
</Shell>